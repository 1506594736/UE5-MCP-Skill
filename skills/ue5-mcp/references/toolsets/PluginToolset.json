{
  "toolsets": [
    {
      "type": "C++",
      "id": "UPluginToolset",
      "desc": "Tools for creating, editing, enabling, and querying Unreal plugins",
      "tools": [
        {
          "id": "ListEnabledPlugins",
          "desc": "Lists the names of all enabled plugins, sorted alphabetically. @return Sorted array of enabled plugin names.",
          "signature": "UPluginToolset.ListEnabledPlugins() -> TArray<FString>"
        },
        {
          "id": "ListDiscoveredPlugins",
          "desc": "Lists the names of all discovered plugins (enabled and disabled), sorted alphabetically. @return Sorted array of discovered plugin names.",
          "signature": "UPluginToolset.ListDiscoveredPlugins() -> TArray<FString>"
        },
        {
          "id": "GetPluginInfo",
          "desc": "Gets metadata for a discovered plugin, including description, version, base directory, content directory, descriptor path, and mounted asset path. @param PluginName The name of the plugin. @return Metadata for the plugin.",
          "signature": "UPluginToolset.GetPluginInfo(const FString&) -> FPluginToolsetInfo"
        },
        {
          "id": "IsEnabled",
          "desc": "Checks whether a discovered plugin is currently enabled. @param PluginName The name of the plugin. @return True if the plugin is enabled, false if it is disabled.",
          "signature": "UPluginToolset.IsEnabled(const FString&) -> bool"
        },
        {
          "id": "GetPluginDependencies",
          "desc": "Returns the dependency entries from a plugin's Plugins array in its .uplugin file. @param PluginName The name of the plugin. @return Array of dependency entries declared in the plugin's .uplugin file.",
          "signature": "UPluginToolset.GetPluginDependencies(const FString&) -> TArray<FPluginDependencyToolsetInfo>"
        },
        {
          "id": "GetPluginDependents",
          "desc": "Returns the names of all discovered plugins that declare a dependency on the given plugin. @param PluginName The name of the plugin to search for as a dependency target. @return Sorted array of plugin names that list the given plugin in their Plugins array.",
          "signature": "UPluginToolset.GetPluginDependents(const FString&) -> TArray<FString>"
        },
        {
          "id": "GetPluginForAsset",
          "desc": "Returns the name of the enabled plugin whose content mount point contains the given asset path. Accepts full asset paths or mount point prefixes (e.g. /PluginName/ or /Game/Path/To/Asset). @param AssetPath The asset or mount point path to look up. @return The name of the plugin that owns the asset.",
          "signature": "UPluginToolset.GetPluginForAsset(const FString&) -> FString"
        },
        {
          "id": "GetPluginTemplateDescriptions",
          "desc": "--- Plugin Templates  ---",
          "signature": "UPluginToolset.GetPluginTemplateDescriptions() -> TArray<FPluginTemplateDescriptionToolsetInfo>"
        },
        {
          "id": "IsPluginCreationAllowed",
          "desc": "Checks whether the editor settings permit plugin creation from the plugin browser. @return True if plugin creation is allowed, false if it is disabled in Editor Settings.",
          "signature": "UPluginToolset.IsPluginCreationAllowed() -> bool"
        },
        {
          "id": "ValidateNewPluginNameAndLocation",
          "desc": "Validates that PluginName and RelativePluginLocation are acceptable for a new plugin. @param PluginName The proposed plugin name. @param RelativePluginLocation Parent directory for the new plugin relative to template's default location. This should be empty unless you wish to specify a subdirectory. @param bPlaceInEngine Use Engine Plugins directory rather than Game Plugins directory location. Only some Templates allow placing in Engine. See the TemplateInfo's bCanBePlacedInEngine. This should be false unless explicitly requested by the user. @param TemplateInfo The plugin template to potentially create from. @return True if the name and location are valid.",
          "signature": "UPluginToolset.ValidateNewPluginNameAndLocation(const FString&, const FString&, const bool, const FPluginTemplateDescriptionToolsetInfo&) -> bool"
        },
        {
          "id": "CreatePlugin",
          "desc": "Creates a new plugin from a template and loads it into the editor. Use GetPluginTemplateDescriptions to obtain a valid TemplateInfo. @param PluginName Name for the new plugin. @param RelativePluginLocation Parent directory for the new plugin relative to template's default location. This should be empty unless you wish to specify a subdirectory. @param bPlaceInEngine Use Engine Plugins directory rather than Game Plugins directory location. Only some Templates allow placing in Engine. See the TemplateInfo's bCanBePlacedInEngine. This should be false unless explicitly requested by the user. @param TemplateInfo The plugin template to create from. @param Description A description for the new plugin. @return Created plugin's descriptor filename. Empty on failure.",
          "signature": "UPluginToolset.CreatePlugin(const FString&, const FString&, const bool, const FPluginTemplateDescriptionToolsetInfo&, const FString&) -> FString"
        },
        {
          "id": "IsPluginModificationAllowed",
          "desc": "Checks whether the editor settings permit modifying plugins from the plugin browser. @return True if plugin modification is allowed, false if it is disabled in Editor Settings.",
          "signature": "UPluginToolset.IsPluginModificationAllowed() -> bool"
        },
        {
          "id": "SetPluginEnabled",
          "desc": "Enables or disables a plugin in the project config. The change takes effect on the next editor restart. @param PluginName The name of the plugin. @param bEnabled True to enable, false to disable.",
          "signature": "UPluginToolset.SetPluginEnabled(const FString&, bool) -> void"
        },
        {
          "id": "GetPluginDescriptor",
          "desc": "Gets the editable descriptor fields for a discovered plugin. @param PluginName The name of the plugin. @return The plugin's current editable descriptor fields.",
          "signature": "UPluginToolset.GetPluginDescriptor(const FString&) -> FPluginDescriptorToolsetInfo"
        },
        {
          "id": "UpdatePluginDescriptor",
          "desc": "Updates a plugin's descriptor fields and writes them to its .uplugin file. Checks out the file via source control if source control is enabled. No-ops if the serialized descriptor is unchanged (file is not touched). @param PluginName The name of the plugin to update. @param NewDescriptor The new descriptor field values to apply.",
          "signature": "UPluginToolset.UpdatePluginDescriptor(const FString&, const FPluginDescriptorToolsetInfo&) -> void"
        },
        {
          "id": "AddPluginDependency",
          "desc": "Adds a dependency entry to a plugin's Plugins array in its .uplugin file. No-ops if a dependency with that name already exists with matching settings. The dependency plugin does not need to be currently discovered. @param PluginName The name of the plugin to modify. @param DependencyName The name of the plugin to add as a dependency. @param bOptional Whether the dependency is optional. @param bEnabled Whether the dependency should be enabled.",
          "signature": "UPluginToolset.AddPluginDependency(const FString&, const FString&, bool, bool) -> void"
        },
        {
          "id": "RemovePluginDependency",
          "desc": "Removes a dependency entry from a plugin's Plugins array in its .uplugin file. @param PluginName The name of the plugin to modify. @param DependencyName The name of the dependency to remove.",
          "signature": "UPluginToolset.RemovePluginDependency(const FString&, const FString&) -> void"
        }
      ]
    }
  ],
  "skills": []
}
