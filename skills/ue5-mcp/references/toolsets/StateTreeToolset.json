{
  "toolsets": [
    {
      "type": "Python",
      "id": "StateTreeTools",
      "desc": "Inspect StateTree (ST) assets.",
      "tools": [
        {
          "id": "get_editor_data",
          "desc": "Returns the editor data for a StateTree asset. Args: state_tree: The StateTree asset. Returns: The editor data object.",
          "signature": "StateTreeTools.get_editor_data(unreal.StateTree) -> unreal.StateTreeEditorData"
        },
        {
          "id": "get_root_states",
          "desc": "Returns top-level states of a StateTree. Args: state_tree: The StateTree asset. Returns: Root-level states.",
          "signature": "StateTreeTools.get_root_states(unreal.StateTree) -> list[unreal.StateTreeState]"
        },
        {
          "id": "get_children",
          "desc": "Returns child states of a state. Args: state: The state to query. Returns: Child states.",
          "signature": "StateTreeTools.get_children(unreal.StateTreeState) -> list[unreal.StateTreeState]"
        },
        {
          "id": "get_tasks",
          "desc": "Returns tasks on a state. Args: state: The state to query. Returns: Task editor nodes.",
          "signature": "StateTreeTools.get_tasks(unreal.StateTreeState) -> list[unreal.StateTreeEditorNode]"
        },
        {
          "id": "get_enter_conditions",
          "desc": "Returns enter conditions on a state. Args: state: The state to query. Returns: Condition editor nodes.",
          "signature": "StateTreeTools.get_enter_conditions(unreal.StateTreeState) -> list[unreal.StateTreeEditorNode]"
        },
        {
          "id": "get_transitions",
          "desc": "Returns transitions on a state. Args: state: The state to query. Returns: Transitions.",
          "signature": "StateTreeTools.get_transitions(unreal.StateTreeState) -> list[unreal.StateTreeTransition]"
        },
        {
          "id": "get_global_tasks",
          "desc": "Returns global tasks that run across all states. Args: state_tree: The StateTree asset. Returns: Global task editor nodes.",
          "signature": "StateTreeTools.get_global_tasks(unreal.StateTree) -> list[unreal.StateTreeEditorNode]"
        },
        {
          "id": "get_evaluators",
          "desc": "Returns global evaluators. Args: state_tree: The StateTree asset. Returns: Evaluator editor nodes.",
          "signature": "StateTreeTools.get_evaluators(unreal.StateTree) -> list[unreal.StateTreeEditorNode]"
        },
        {
          "id": "get_node_description",
          "desc": "Returns a human-readable description for a node. Args: state_tree: The StateTree asset. node: The editor node to describe. Returns: Formatted description string.",
          "signature": "StateTreeTools.get_node_description(unreal.StateTree, unreal.StateTreeEditorNode) -> str"
        }
      ]
    }
  ],
  "skills": []
}
