{
  "toolsets": [
    {
      "type": "C++",
      "id": "UDataflowAgentToolset",
      "desc": "Dataflow Agent Toolset - exposes Dataflow graph editing operations as AI agent tools.",
      "tools": [
        {
          "id": "CreateGraph",
          "desc": "Creates a new saved Dataflow graph asset. @param Name - Name for the new Dataflow asset (e.g., \"DF_Simulation\") @param Path - Content folder path where the asset should be created. Defaults to \"/Game/Dataflow\" @return The full asset path of the created graph, or empty string on failure",
          "signature": "UDataflowAgentToolset.CreateGraph(const FString&, const FString&) -> FString"
        },
        {
          "id": "GetGraphStructure",
          "desc": "Returns the complete structure of a Dataflow graph including all nodes and connections. @param Graph - The Dataflow asset to inspect @return JSON string with graph structure (nodes + connections), or error message on failure",
          "signature": "UDataflowAgentToolset.GetGraphStructure(const UDataflow*) -> FString"
        },
        {
          "id": "ListNodeTypes",
          "desc": "Returns a JSON list of all registered Dataflow node types. @param bCommonOnly - When true, only return non-deprecated non-experimental nodes. Default is true. @return JSON array of node type names",
          "signature": "UDataflowAgentToolset.ListNodeTypes(const bool) -> FString"
        },
        {
          "id": "GetNodeTypeSchema",
          "desc": "Returns the schema for a Dataflow node type including its input/output pins and editable UPROPERTY parameters. @param TypeName - The node type name (e.g., \"FAddFloatsDataflowNode\") @return JSON object with name, category, tooltip, inputPins, outputPins, and properties",
          "signature": "UDataflowAgentToolset.GetNodeTypeSchema(const FString&) -> FString"
        },
        {
          "id": "AddNode",
          "desc": "Adds a node of the given type to the Dataflow graph. @param Graph      - The Dataflow asset to add the node to @param TypeName   - The node type name (e.g., \"FAddFloatsDataflowNode\") @param NodeName   - Unique name for the node within this graph @param JsonParams - Optional JSON object of property overrides (e.g., {\"Value\": 3.14}) @param X          - X position in the graph editor (default 0, typical node width ~200) @param Y          - Y position in the graph editor (default 0, typical node height ~100) @return The newly created UDataflowEdNode, or nullptr on failure",
          "signature": "UDataflowAgentToolset.AddNode(UDataflow*, const FString&, const FString&, const FString&, const int32, const int32) -> UDataflowEdNode*"
        },
        {
          "id": "UpdateNode",
          "desc": "Updates an existing node's editable properties via JSON. @param Node       - The EdNode to modify @param JsonParams - JSON object of property overrides (e.g., {\"Value\": 3.14})",
          "signature": "UDataflowAgentToolset.UpdateNode(UDataflowEdNode*, const FString&) -> void"
        },
        {
          "id": "GetNodeInfo",
          "desc": "Returns information about a node as a JSON object (name, type, position, pins). @param Node - The EdNode to query @return JSON string with node info, or error message on failure",
          "signature": "UDataflowAgentToolset.GetNodeInfo(UDataflowEdNode*) -> FString"
        },
        {
          "id": "RepositionNode",
          "desc": "Moves a node to a new position in the graph editor. @param Node - The EdNode to reposition @param X    - New X position @param Y    - New Y position",
          "signature": "UDataflowAgentToolset.RepositionNode(UDataflowEdNode*, const int32, const int32) -> void"
        },
        {
          "id": "RemoveNode",
          "desc": "Removes a node and all its connections from the Dataflow graph. @param Graph - The Dataflow asset containing the node @param Node  - The EdNode to remove",
          "signature": "UDataflowAgentToolset.RemoveNode(UDataflow*, UDataflowEdNode*) -> void"
        },
        {
          "id": "ConnectNodePins",
          "desc": "Connects an output pin of one node to an input pin of another. @param FromNode - The source EdNode @param FromPin  - Name of the output pin on the source node @param ToNode   - The destination EdNode @param ToPin    - Name of the input pin on the destination node @return true on success, false on failure",
          "signature": "UDataflowAgentToolset.ConnectNodePins(UDataflowEdNode*, const FString&, UDataflowEdNode*, const FString&) -> bool"
        },
        {
          "id": "DisconnectNodePins",
          "desc": "Removes the connection between two node pins. @param FromNode - The source EdNode @param FromPin  - Name of the output pin on the source node @param ToNode   - The destination EdNode @param ToPin    - Name of the input pin on the destination node",
          "signature": "UDataflowAgentToolset.DisconnectNodePins(UDataflowEdNode*, const FString&, UDataflowEdNode*, const FString&) -> void"
        },
        {
          "id": "ListVariables",
          "desc": "Returns all variables defined on the Dataflow graph as a JSON array. Each entry contains \"name\", \"type\", and \"value\" fields. @param Graph - The Dataflow asset to inspect @return JSON array of variable descriptors, or empty string on failure",
          "signature": "UDataflowAgentToolset.ListVariables(const UDataflow*) -> FString"
        },
        {
          "id": "AddVariable",
          "desc": "Adds a new variable to the Dataflow graph. Supported type strings: Primitives : \"Bool\", \"Int32\", \"Int64\", \"Float\", \"Double\", \"Name\", \"String\" Structs    : UScriptStruct name with or without the \"F\" prefix e.g. \"Vector\", \"FVector\", \"Transform\", \"FTransform\", \"Rotator\", \"LinearColor\" Objects    : \"Object:<ClassName>\" where ClassName is with or without the \"U\"/\"A\" prefix e.g. \"Object:StaticMesh\", \"Object:USkeletalMesh\" @param Graph - The Dataflow asset to modify @param Name  - Unique name for the new variable @param Type  - Type string as described above @return true on success, false if the variable already exists or the type cannot be resolved",
          "signature": "UDataflowAgentToolset.AddVariable(UDataflow*, const FString&, const FString&) -> bool"
        },
        {
          "id": "RemoveVariable",
          "desc": "Removes a variable from the Dataflow graph. @param Graph - The Dataflow asset to modify @param Name  - Name of the variable to remove",
          "signature": "UDataflowAgentToolset.RemoveVariable(UDataflow*, const FString&) -> void"
        },
        {
          "id": "SetVariable",
          "desc": "Sets the value of an existing variable using its serialized string representation. The format depends on the variable's type (e.g., \"3.14\" for float, \"true\" for bool, \"42\" for int, \"MyName\" for FName). @param Graph - The Dataflow asset to modify @param Name  - Name of the variable to set @param Value - Serialized string value to assign @return true on success, false if the variable is not found or the value cannot be parsed",
          "signature": "UDataflowAgentToolset.SetVariable(UDataflow*, const FString&, const FString&) -> bool"
        },
        {
          "id": "AddCommentBox",
          "desc": "Adds a comment box around the given nodes. @param Graph   - The Dataflow asset to add the comment to @param Nodes   - List of EdNodes to surround with the comment box @param Comment - Text to display on the comment box @param Color   - Background color of the comment box (defaults to White) @return Node ID string of the created comment node, or empty string on failure",
          "signature": "UDataflowAgentToolset.AddCommentBox(UDataflow*, const TArray<UDataflowEdNode*>&, const FString&, FLinearColor) -> FString"
        },
        {
          "id": "RemoveCommentBox",
          "desc": "Removes a comment box node from the graph. @param Graph     - The Dataflow asset containing the comment @param CommentId - The node GUID string returned by AddCommentBox",
          "signature": "UDataflowAgentToolset.RemoveCommentBox(UDataflow*, const FString&) -> void"
        },
        {
          "id": "ListDataflowCompatibleAssetTypes",
          "desc": "Returns a JSON list of every UClass that can host an embedded Dataflow graph (i.e. implements IDataflowInstanceInterface). Each entry has \"className\", \"displayName\", and \"modulePath\" fields. Use the \"className\" value as input to CreateDataflowCompatibleAsset or ListDataflowTemplatesForAssetClass. @return JSON array of compatible asset type descriptors",
          "signature": "UDataflowAgentToolset.ListDataflowCompatibleAssetTypes() -> FString"
        },
        {
          "id": "CreateDataflowCompatibleAsset",
          "desc": "Creates a new Dataflow-compatible asset (e.g. ChaosClothAsset, GeometryCollection, FleshAsset, GroomAsset) with an empty embedded Dataflow graph. @param ClassName - Asset class name with or without the \"U\"/\"A\" prefix (e.g. \"ChaosClothAsset\" or \"UChaosClothAsset\") @param Name      - Name for the new asset @param Path      - Content folder path where the asset should be created. Defaults to \"/Game/Dataflow\" @return The full asset path of the created asset, or empty string on failure",
          "signature": "UDataflowAgentToolset.CreateDataflowCompatibleAsset(const FString&, const FString&, const FString&) -> FString"
        },
        {
          "id": "CreateDataflowCompatibleAssetFromTemplate",
          "desc": "Creates a new Dataflow-compatible asset and initialises its embedded Dataflow graph from a registered template in one step. @param ClassName  - Asset class name with or without the \"U\"/\"A\" prefix @param Name       - Name for the new asset @param Path       - Content folder path @param TemplateId - Template identifier returned by ListDataflowTemplatesForAssetClass. Pass an empty string to create with an empty embedded graph (equivalent to CreateDataflowCompatibleAsset). @return The full asset path of the created asset, or empty string on failure",
          "signature": "UDataflowAgentToolset.CreateDataflowCompatibleAssetFromTemplate(const FString&, const FString&, const FString&, const FString&) -> FString"
        },
        {
          "id": "ListDataflowTemplatesForAssetClass",
          "desc": "Returns a JSON list of Dataflow templates registered for the given asset class. Templates registered for parent classes are included (class hierarchy walk). @param ClassName     - Asset class name with or without the \"U\"/\"A\" prefix @param bIncludeBlank - When true, include a \"Blank\" option (empty graph) at the start @return JSON array of `{ \"templateId\", \"displayName\", \"tooltip\" }` objects. \"templateId\" is opaque - pass it back to AssignDataflowTemplate or CreateDataflowCompatibleAssetFromTemplate.",
          "signature": "UDataflowAgentToolset.ListDataflowTemplatesForAssetClass(const FString&, const bool) -> FString"
        },
        {
          "id": "AssignDataflowTemplate",
          "desc": "Assigns a Dataflow template to an existing Dataflow-compatible asset by duplicating the template graph and embedding it. Replaces any existing embedded graph. @param Asset      - The target asset (must implement IDataflowInstanceInterface) @param TemplateId - Template identifier returned by ListDataflowTemplatesForAssetClass. Pass an empty string to assign a fresh empty graph. @return true on success, false on failure",
          "signature": "UDataflowAgentToolset.AssignDataflowTemplate(UObject*, const FString&) -> bool"
        }
      ]
    }
  ],
  "skills": []
}
